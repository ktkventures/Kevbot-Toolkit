# RoR Trader Dependencies

# Data Provider
alpaca-py>=0.21.0

# Data Processing
# PINNED to the known-good shadow-worker container versions (2026-07-18): the unpinned
# `>=` ranges let a fresh build pull a newer pandas/numpy than the running 07-09 image
# (pandas 3.0.3 / numpy 2.5.1), and the mismatch crashed the shadow-worker at startup.
# Local dev venvs may lag (pandas 2.x) — keep them in sync when validating fidelity.
pandas==3.0.3
numpy==2.5.1
pyarrow==24.0.0

# Process metrics (data-worker memory instrumentation)
psutil>=5.9.0

# Technical Analysis (for indicator calculations)
ta>=0.11.0

# Web Framework (for MVP)
streamlit>=1.32.0

# Visualization
plotly>=5.18.0
# streamlit-lightweight-charts fork installed separately
# Local:  pip install -e ./streamlit_lwc_fork
# Docker: handled in Dockerfile

# Database & Auth (Supabase PostgreSQL + Row Level Security)
supabase==2.31.0   # PINNED to known-good (see Data Processing note)

# HTTP (webhooks)
requests>=2.31.0

# Timezone support
pytz>=2024.1

# Environment Variables
python-dotenv>=1.0.0

# FastAPI (Phase 38 — API layer for Next.js frontend)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-jose[cryptography]>=3.3.0

# AI Providers (Pack Builder)
anthropic>=0.40.0
openai>=1.40.0

# M-RS2 REST Bars direct-Postgres reader (bar_cache.read_bars)
psycopg[binary]>=3.1
