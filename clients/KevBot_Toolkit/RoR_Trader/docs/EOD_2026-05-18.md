# EOD 2026-05-18 — Phase H concluded, live_bars consolidated, grace shadow staged

A big day. Three threads: **Phase H wrapped**, the **`live_bars` cache
consolidated to a single writer**, and the **10Sec builder fix staged for
tomorrow's open**.

## Resume point — 2026-05-19 at the open

The one active task: **pick the 10Sec grace window.** Full plan in
`docs/Grace_Window_Investigation_2026-05-18.md`. Short version:

1. ~15 min after the open, check Worker logs for `A-channel lag (30s
   window)` — confirm the measured ~3s holds under RTH load (today's
   sample was thin after-hours).
2. Bars Comparison → SPY or TSLA, **Timeframe = 10Sec**, A/B each
   `Grace 2s / 3s / 4s` source against `REST 1Sec aggs`.
3. Pick the smallest grace that hits ~99% close-match (predicted: **3s**).
4. Promote that grace into `force_close_stale_bar` (`ralph_engine.py`) —
   that is the actual **Bug B** fix.

## What shipped today (commits on `dev`)

| Commit | What |
|---|---|
| `d70fdf2` | Phase H trade-shadow instrumentation |
| `dc90460` | Phase H conclusion doc — Option C, trade-channel shelved |
| `46b0c37` | `_cache_rest_parity.py` stress-test script |
| `7e5b023` | **Stage A** — drop stock `AM.*` sub; ws_agg sole `live_bars` writer; `ws_with_corrections` retired |
| `1bc00b1` | **Stage B** — REST backfill cron (`live_bars_rest_backfill.py`) |
| `94dccc1` | `/cache-bars` `source` filter (`all` / `live`) |
| `4b2df71` | "Cache + backfill" source in Bars Comparison |
| `ba560f1` | **Bug A** fix (late-bar drop) + backfill extended to derived ≥60s TFs |
| `f501126` | A-channel ingestion-lag instrumentation |
| `ad7b996` | **Grace-window shadow** — 2s/3s/4s 10Sec bars + UI sources |
| `a5705f6` | Grace shadow hotfix (volume bigint) |

## State of things

- **Phase H — CONCLUDED (Option C).** Trade-channel self-built bars
  shelved: the worker's ingestion lag exceeds Polygon's rebroadcast lag.
  The decision-time cache is the production source. `trade_bar_builder.py`
  / `live_bars_trades` left inert.
- **`live_bars` — single writer.** `ws_agg` is the sole ≥60s writer (AM
  subscription dropped). The `first_*` decision-time columns are clean
  again. Verified: post-cutover decision-time cache ≈ **94.6%** vs REST
  1Min, uniform across SPY/TSLA/META/AAPL/AMD (the ~physics ceiling).
- **REST backfill — live.** Fills 1Min gaps AND re-derives missing ≥60s
  bars. `LIVE_BARS_BACKFILL_ENABLED=true` on the Worker.
- **1Min / 5Min cache — pristine** (100% close-match vs REST today).
- **10Sec cache — Bug A fixed, Bug B staged.** Grace shadow collecting
  2s/3s/4s variants for tomorrow's comparison.

## Worker env vars (Railway, current)
- `POLYGON_TRADE_SHADOW_ENABLED=false` (Phase H shelved)
- `LIVE_BARS_BACKFILL_ENABLED=true`
- `GRACE_SHADOW_ENABLED` — default on (no var set)

## Backup branches
- `backup-2026-05-18-100pct-cache-rest-match`
- `backup-pre-live-bars-consolidation-2026-05-18`
- `backup-pre-grace-shadow-2026-05-18`
- `backup-eod-2026-05-18` (full EOD state)

## Open / deferred
- **Bug B fix** — promote the winning grace into `force_close_stale_bar`
  (tomorrow, after the comparison).
- **REST-as-live-model** (Stage 2/3) — Kevin's idea: live model reads
  mostly REST, cache only for the last X seconds. Decouples decision
  (cache) from display/indicators (REST). Sound; revisit after 10Sec.
- **Frontend REST 1Min `$480` bug** — the Bars Comparison "REST 1Min"
  source shows stale/wrong data; frontend-side, separate.
- **`size < 1` drop** in `trade_bar_builder` (25–53% of trades) — Phase H
  residual; low priority since the trade shadow is off.

## Reference docs
- `docs/Grace_Window_Investigation_2026-05-18.md` — tomorrow's task.
- `docs/Phase_H_Findings_2026-05-15.md` — Phase H conclusion.
- `docs/Claude_App_REST_vs_Cache_Analysis.md` — external-AI thread
  (settlement-window analysis; its "1s" figure is settlement-only — our
  total pipeline lag measured ~3s).
