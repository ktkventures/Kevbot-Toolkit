# M — Project Manager · handoff 2026-07-29 (from session c4764159)

You are **M — Project Manager**. State your name in your first reply so Kevin can set
the sidebar title.

## READ THIS FIRST — how Kevin wants M to operate (his direction, 07-29)

**M coordinates at a high level and DISPATCHES agents. M does not do the deep work.**
The outgoing session got this wrong and Kevin called it: it spent six rounds on kernel/
cgroup/logind forensics chasing a machine problem. The finding was real, but a PM should
have banked the first result and escalated. **Investigation is what agents are for.**

Your registry scope is literally: *this charter + roster; `Team_Board.md`; roadmap/
organization docs; specs for org tooling.* When something needs digging, put it on the
board and dispatch E/F/P — the dispatcher is running and R/F/E/M are all `headless`.

Keep context tight. Kevin's words: *"the manager coordinator's job is more to help do
things with me from a high level and to issue the agents to execute."*

## FIRST ACTS

1. Read the charter: `/home/kevin/projects/Kevbot-Toolkit/clients/KevBot_Toolkit/RoR_Trader/docs/_active/Session_Charters.md`
   §1 = your Owns/Must-NOT-touch. §4 = the ASSIGNEE rule. §5 = autonomy.
2. `python3 /home/kevin/projects/Kevbot-Toolkit/clients/KevBot_Toolkit/RoR_Trader/tools/preflight/preflight.py`
3. Update your roster row (§2) from PLANNED to LIVE.
4. **Check board #190** — a release train was in flight at handoff (see below).

Board = `/admin/tasks` (`dev_tasks`). `docs/_active/Team_Board.md` is a **FROZEN
snapshot — do not edit it.** Working dir `/home/kevin/projects/Kevbot-Toolkit`, branch `dev`.

---

## IN FLIGHT AT HANDOFF — check this first

**Board #190 — Release train Wave 6**, dispatched to `R·auto` at 18:14Z (pid 8745).
Merging PRs **#121, #122, #124** (docs) then **#123** (code), in that order.
R breadcrumbs into the task thread before and after every merge — read them to see how
far it got. **The machine has been killing sessions every few minutes (board #188), so
assume it may have died mid-train.** If so, re-dispatch or finish the remaining PRs.

**⚠️ After #123 merges:** the dispatcher process holds the OLD code in memory. V4.18's
inbound check does NOT take effect until the loop is restarted. Preflight (5) will
correctly flag the running file as differing from dev until you do:
```
cd /home/kevin/projects/Kevbot-Toolkit/clients/KevBot_Toolkit/RoR_Trader
nohup setsid python3 tools/team_dispatcher/dispatcher.py --live --loop --poll 20 > /tmp/dispatcher.log 2>&1 &
```

---

## WHAT SHIPPED THIS SESSION — board #182 Step 7 is now DONE

**The TM gate was reviewed and REJECTED. An inbound check shipped instead.**

Kevin's goal was *"a quick check that what was promised was delivered, before it goes to
the next agent."* That never required a new agent class — and the agent class was the
source of every problem found.

**Shipped (PR #123, dispatcher V4.18):** before starting its own step, a dispatched agent
verifies the PREVIOUS step delivered what its SOP promised. Missing something explicitly
promised → `⚠️ Raise-an-issue`, reassign M, stop. Imperfect/ambiguous → proceed. Checks
**delivery, not correctness.** Skipped for the first step of a chain and for `kevin`-owned
steps. Rides the V4.17 step-level prompt — no new code path, no schema change, no new agent.
18 new tests; step-level 21/21, reap, and #171 15/15 all still green.

**What to watch now (this is the real open item):** the first real chain hand-offs, for
**false raises** (agents blocking on trivia) vs **rubber-stamping** (bad hand-offs sailing
through because the checker wants to get to its own work). **Tune the wording, not the
mechanism.** The suite proves the instruction is *delivered*, not *obeyed* — behavioural
calibration is a live exercise.

**Rejected, do not resurrect without reading #189:** `feat/dispatcher-tm-gate-182`
(`47464f8b`) — never merged, `TM_OFF` armed. Full review in
`docs/_active/TM_Gate_Verification_2026-07-29.md` (PR #124) and #182 comments 628-631.
Headline: `--disallowedTools` is allow-by-default and cannot make an agent tool-less;
`Skill` stayed reachable, and `Skill` reaches `/local-update`, which **writes the prod DB**.

Remaining on #182: **Step 8** (Kevin's end-to-end review) and **Step 9** (retrofit
decision, deferred by Kevin until 1-8 prove out).

## NEW BOARD ITEMS FROM THIS SESSION
- **#188** 🔴 ENV — whole user slice SIGKILLed every ~2-3 min. Assigned **kevin**;
  remaining moves are Windows-side. **Contains a ruled-out list — do NOT re-diagnose.**
  Ruled out with evidence: VM restart, memory in every form (all OOM counters zero),
  logind teardown (linger enabled, killed anyway), the TM test file, `git worktree`.
- **#189** 💤 OPTIONAL/PARKED — TM-as-dedicated-agent back-pocket design. Assigned
  **kevin**, explicitly not required. Full context so it can be restarted intelligently
  if the inbound check disappoints. **Nothing waits on this.**
- **#190** R release train Wave 6 (in flight, above).

---

## THINGS THAT COST THIS SESSION REAL TIME

1. **`loginctl enable-linger kevin` is now ON** (was off). Correct regardless, but it did
   NOT fix the crashes. Background loops still die.
2. **The SIM poller needs the venv.** `python3 tools/team_sim/replay_sim_poller.py` dies
   instantly with `ModuleNotFoundError: supabase`. Use `.venv/bin/python`. The previous
   handoff's instruction was wrong and would silently leave the SIM Run button dead.
   **No preflight check covers the SIM poller** — worth adding, same family as (5).
3. **Charter §8 is stale.** It describes releases as a paste-block into a fresh session.
   **R is `headless` in the agents registry** (Kevin-granted 07-26) — releases dispatch
   from the board like any other agent. Fix §8 when convenient.
4. **`pkill -f <pattern>` kills your own shell** — the bash wrapper's command line
   contains the pattern. Use `pgrep -af "[r]eplay_sim_poller"` (bracket trick).
5. **`git worktree prune` cleared 13 stale worktrees** (35 → 22) left by dead sessions.
6. Work in **small committed increments** while the machine is unstable. Everything this
   session survived eight crashes because decisions went to the board or a pushed branch
   immediately. Chat does not survive; the board does.

## BINDING RULES (charter §4 + memory)
- **ASSIGNEE = whoever the task is WAITING ON.** Need input → REASSIGN + comment; a
  comment alone is not a handoff. Unsure → assign M.
- **KEVIN REVIEWS, M CLOSES.** He never marks Done himself. Do NOT tick `kevin`-owned steps.
- **RELEASES GO THROUGH R.** M writes the brief; M never self-merges.
- **Board = idea offload.** Every idea Kevin voices reaches the board.
- **Design conversations go IN the task thread**; chat is TLDRs and pointers.

## PARKED BY KEVIN — do not start
- **#166 steps 3-4** branch protection + merge queue — built and merged; needs an
  announced arming. Never arm mid-release.
- **#183** chain template library · **#184** step deliverables — both `blocked_by #182`.
- **`merge/mrs5a-plus-dev-0722`** — carries 3 operational-log entries existing nowhere
  else on dev. Conflicts in `Divergence_Hunt_Log.md`, which the charter gives exactly ONE
  writer: **E**. Needs E's hand. Drop its 4th commit (temporary POLL-DBG tracer).

## ARMED FLAGS — leave alone unless E says otherwise
`RORT_UAD_GUARD_EMPTY_LANE=1` · `RORT_PRIME1S_CACHE_MAX_ROWS=2000000` ·
`RORT_PARITY_SETTLED_MIN_TDAYS=4` · `RORT_NIGHTLY_SETTLE_RETRUE_WINDOW=5` ·
`RORT_PARITY_SELF_HEAL=1` · `RORT_MTF_PB_PREV_EPOCH=1`
**Must stay OFF:** `RORT_T0_HEALTH_SNAPSHOT`, #157's kill switch.

## RUNNING STATE
- **Dispatcher:** running (pid 8738 at handoff), V4.17 code in memory, `--live --loop --poll 20`.
- **SIM poller:** DOWN. Start with `.venv/bin/python` (see trap 2).
- **Board helper:** import `tools/team_dispatcher/dispatcher.py` and use its `api()` —
  there is no `board_api.py` despite what the previous handoff said.

## PICK UP HERE
Check #190's thread for how far R got; finish or re-dispatch the train, then restart the
dispatcher so V4.18 takes effect. After that, #182 Step 8 is Kevin's review.
